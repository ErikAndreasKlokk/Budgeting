# PostgreSQL Database Connection
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/budgeting

# For external database connections, use:
# DATABASE_URL=postgresql://username:password@your-db-host.com:5432/budgeting

# Optional: Node environment (production for deployed instances)
# NODE_ENV=production

# Optional: Server origin for SvelteKit (required in production)
# ORIGIN=https://your-domain.com

# ============================================
# Docker Compose Environment Variables
# ============================================
# Used by docker-compose.yml for the bundled PostgreSQL database
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_DB=budgeting

# Used by docker-compose.external-db.yml for external database
# POSTGRES_HOST=your-db-host.com
# POSTGRES_PORT=5432
